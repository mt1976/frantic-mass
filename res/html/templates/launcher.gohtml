<!DOCTYPE html>
<html lang="en">
   {{template "head" .}}
    <body>
        <form action="#" method="POST" id="launcherForm">
            {{template "handleform" .}}
            <hgroup>
                <p>Welcome to the <i>{{.Context.AppName}}</i> Launcher Page</p>
            </hgroup>
            {{template "breadcrumbs" .}}
            <article>
                <p>{{.Context.AppDescription}}</p>
                <p>Click below to start {{.Context.AppName}}.</p>
            </article>
        {{template "chart" .}}
        {{template "alerts" .}}
        {{template "actions" .Context.PageActions}}
        </form>
    </body>
    {{template "footer" .}}
    {{template "scripts" .}}
</html>