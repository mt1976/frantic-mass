<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{{.Context.AppName}} - {{.Context.PageTitle}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <style>
            body {
                font-family: Arial, sans-serif;
                background: #181c20;
                color: #fff;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                height: 100vh;
                margin: 0;
            }
            .launcher-container {
                background: #23272b;
                padding: 2rem 3rem;
                border-radius: 12px;
                box-shadow: 0 4px 24px rgba(0,0,0,0.3);
                text-align: center;
            }
            .launcher-title {
                font-size: 2rem;
                margin-bottom: 1rem;
            }
            .launcher-btn {
                background: #4f8cff;
                color: #fff;
                border: none;
                padding: 0.75rem 2rem;
                border-radius: 6px;
                font-size: 1rem;
                cursor: pointer;
                transition: background 0.2s;
            }
            .launcher-btn:hover {
                background: #3766c3;
            }
        </style>
    </head>
    <body>
        <div class="launcher-container">
            <div class="launcher-title">Welcome to the {{.Context.AppName}} Launcher</div>
            <p>Click below to start {{.Context.AppName}}.</p>
            <p>{{.Context.AppDescription}}</p>
            <!-- Range through the page actions from the context -->
            {{if .Context.PageActions.Actions}}
              
                    {{range .Context.PageActions.Actions}}
                            <button class="launcher-btn" onclick='{{.OnClick}}'>{{.Name}}</button>
                    {{end}}
         
            {{else}}
                <p>No actions available.</p>
            {{end}}
            <p><small>Version: {{.Context.AppVersion}}</small></p>
        </div>
        <script>
            function launchApp() {
                alert('Launching {{.Context.AppName}}...');
                // Add your launch logic here
            }
        </script>
    </body>
</html>